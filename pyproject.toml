[project]
name = "hyprvault"
version = "1.0.0"
description = "Hyprland Session Manager"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
hyprvault = "hyprvault.main:main_entry"
